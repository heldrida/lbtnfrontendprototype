console.log('mainMenu.js loaded!');

(function (App) {

	function MainMenu (params) {
		this.init(params);
	}

	MainMenu.prototype = {
		init: function (params) {

		}
	};

	module.exports = MainMenu;

}(window.App));